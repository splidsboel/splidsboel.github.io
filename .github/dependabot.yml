version: 2
updates:
  # npm dependencies (Astro, Tailwind, React, etc.)
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 10
    groups:
      # Bundle routine updates into a single PR to cut noise.
      minor-and-patch:
        update-types:
          - minor
          - patch

  # GitHub Actions used by the deploy workflow
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
